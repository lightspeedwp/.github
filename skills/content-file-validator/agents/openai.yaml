interface:
  display_name: "Content File Validator"
  short_description: "Validate Markdown, frontmatter, and version rules."
  default_prompt: "Use $content-file-validator to validate Markdown, frontmatter schema compliance, SemVer versioning, and consolidated content quality reporting."
