# Copilot Prompts

- Add a new accent: “Add `accent-950` to `settings.color.palette` and update docs.”
- Block style: “Create `.is-style-card` using `spacing-40` padding and `neutral-0` background.”
- Fluid headings: “Refactor H1–H6 to LSX font-size tokens with sensible fluid scale.”
- Mapping help: “Explain how `font-size-400` maps to CSS variables in the editor.”
