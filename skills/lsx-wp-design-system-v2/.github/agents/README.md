# Copilot Agents

- **DesignTokens** — proposes and validates LSX tokens.
- **ThemeJSON** — safely updates theme.json.
- **WPDocs** — drafts documentation for editors/devs.

Use with `copilot-chatmodes.json` to seed context.
