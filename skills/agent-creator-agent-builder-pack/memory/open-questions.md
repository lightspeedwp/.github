# Open Questions

## Blocking open questions

None for this pack.

## Non-blocking review questions

- Should the live `agent-creator` skill package include the helper scripts directly, or should the scripts remain in generated Builder spec packs only?
- Should the default pack omit `assets/` unless the user supplies real reusable assets?

## Validator

Checked by `scripts/validate-memory-hygiene.py`.
