# Example — Memory Update

## Input

"From now on, this agent should default to read-only connector access unless I explicitly approve writes."

## Expected behaviour

Record as durable agent default. Do not store one-off task context.

## Expected output

Updated `memory/agent-defaults.md` entry and validation pass.
