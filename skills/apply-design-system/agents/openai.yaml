interface:
  display_name: Apply Design System [apply-design-system]
  short_description: Review an existing design and connect it to design system components.
  default_prompt: Use $apply-design-system to review this Figma screen if needed and
    then connect the relevant sections to the design system.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
