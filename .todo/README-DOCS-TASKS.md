I need you to create a copilot todo list from the items below:

## Make sure our main README.md is amazing

The main README.md in the root needs to be updated with current information

Also with header and footers + badges + headers need to contain emojis.

The frontmatter should contain references. The footers should all have consistent references listed.

This .github repository has become a very important repository for LightSpeed, it is a single source of truth for all instructions, agent files, chatmodes, prompts, documentation and labeling canonical files, we need to make sure all community health files align with our processes and list frontmatter references and also huiman readable references in the footer, which may differ to the frontmattter.

Lint/format doc for compliance.

## Update .github/ folder governance docs

The .md governance docs in the .github/ folder need to be reviewed for issues. Flag issues for review.

Also update with header and footers + badges + headers need to contain emojis.

The frontmatter should contain references, these will be used by AI. The footers should all have consistent references listed, these will be relevant to humans.

Lint/format all docs for compliance.

## Update all of the root docs/ folder

All of the docs/ folder root documentation files need to align with current state of repo. Critically run through all of the documents and update them to align with our latest processes.

## Update all of our attached Community Health files

The root .md community health files need to align with our latest governance docs in .github and in the docs/ folder.

Update the headers and footers + add relevant badges + add heading emojis where it makes sense.

The frontmatter should contain references for AI benefit. The footers should all have consistent references listed for human consumption.

This .github repository has become a very important repository for LightSpeed, it is a single source of truth for all instructions, agent files, chatmodes, prompts, documentation and labeling canonical files, we need to make sure all community health files align with our processes and list frontmatter references and also huiman readable references in the footer, which may differ to the frontmattter.

Lint/format all docs for compliance.

## Update / Create README.md in docs folders

I want you to update all of the README docs in every folder in docs/ folder and create missing ones if needed.

The frontmatter should contain references, these will be used by AI. The footers should all have consistent references listed, these will be relevant to humans.

Lint/format all docs for compliance.

# docs subfolder folder updates

Review all documentation in all folders, add readme files to every folder

Cross-linking and references should be present present, but some files

Extend and clean up sparse docs

Update/remove the old README.md to reflect the new doc locations.

Lint/format all docs for compliance.

Add/update footers and references for consistency.

Add emojis to headings where it makes sense
