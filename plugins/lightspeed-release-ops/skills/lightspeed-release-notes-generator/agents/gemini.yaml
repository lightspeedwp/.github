interface:
  display_name: LightSpeed Release Notes Generator
  short_description: generate structured release notes from validated changelog and repository signals.
  icon: layers
  brand_color: "#1D4ED8"
policy:
  products:
    - gemini
  allow_implicit_invocation: true
  compatibility_mode: experimental
