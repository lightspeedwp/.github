---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getAllAgents } from "../../lib/agents";

export async function getStaticPaths() {
  return getAllAgents().map((agent) => ({
    params: { slug: agent.slug },
    props: { agent },
  }));
}

const { agent } = Astro.props;

if (!agent) {
  throw new Error("Agent data was not provided.");
}
---

<BaseLayout
  title={`${agent.name} — Awesome GitHub`}
  description={agent.description}
>
  <section class="hero">
    <div class="eyebrow">Agent Guide</div>
    <h1>{agent.name}</h1>
    <p class="lede">{agent.description}</p>
    <div class="hero-actions">
      <a class="button primary" href="/agents/">Back to Agents</a>
    </div>
  </section>

  <section class="section">
    <div class="agent-info">
      <div class="info-block">
        <strong>Difficulty Level</strong>
        <span class="badge">{agent.difficulty}</span>
      </div>
      <div class="info-block">
        <strong>Related Agents</strong>
        <div class="related-links">
          {agent.relatedAgents.map((agentSlug) => (
            <a href={`/agents/${agentSlug}/`} class="related-agent">
              {agentSlug.charAt(0).toUpperCase() + agentSlug.slice(1)} Agent
            </a>
          ))}
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <h2>Overview</h2>
    <div class="page-copy">
      <p>{agent.overview}</p>
    </div>
  </section>

  <section class="section">
    <h2>Key Capabilities</h2>
    <div class="page-copy">
      <ul>
        {agent.capabilities.map((cap) => (
          <li>{cap}</li>
        ))}
      </ul>
    </div>
  </section>

  <section class="section">
    <h2>Integration Points</h2>
    <div class="page-copy">
      <p>
        This agent works alongside other components in the system:
      </p>
      <ul>
        {agent.integrations.map((integration) => (
          <li><strong>{integration}</strong></li>
        ))}
      </ul>
    </div>
  </section>

  <section class="section">
    <h2>Common Use Cases</h2>
    <div class="page-copy">
      <ul>
        {agent.useCases.map((useCase) => (
          <li>{useCase}</li>
        ))}
      </ul>
    </div>
  </section>

  <section class="section">
    <h2>Explore More</h2>
    <div class="page-copy">
      <ul>
        <li>
          <a href="/agents/">All Agent Guides</a> — Compare capabilities
        </li>
        <li>
          <a href="/getting-started/">Implementation Roadmap</a> — Adoption
          sequence
        </li>
        <li>
          <a href="/references/glossary/">Glossary</a> — Key terminology
        </li>
      </ul>
    </div>
  </section>
</BaseLayout>

<style>
  .agent-info {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 1.5rem;
    margin: 0;
  }

  .info-block {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 1rem;
  }

  .info-block strong {
    display: block;
    margin-bottom: 0.75rem;
    font-size: 0.875rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text);
  }

  .badge {
    display: inline-block;
    padding: 0.375rem 0.75rem;
    background: var(--bg-soft);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    font-size: 0.875rem;
    color: var(--accent);
    font-weight: 600;
  }

  .related-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .related-agent {
    display: inline-block;
    padding: 0.375rem 0.75rem;
    background: var(--bg-soft);
    border: 1px solid var(--border);
    border-radius: var(--radius-md);
    color: var(--accent);
    text-decoration: none;
    font-size: 0.875rem;
    font-weight: 500;
    transition: all 0.2s ease-out;
  }

  .related-agent:hover {
    background: var(--accent);
    color: var(--bg);
    border-color: var(--accent);
  }
</style>
