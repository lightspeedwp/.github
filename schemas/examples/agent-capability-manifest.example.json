{
  "capabilities": [
    {
      "name": "playwright-spec-generation",
      "description": "Generate @playwright/test specs from an approved test pack.",
      "category": "core",
      "requires": {
        "libraries": ["@playwright/test"],
        "nodejs_version": ">=18.0.0"
      },
      "performance_targets": { "max_latency_ms": 5000 }
    },
    {
      "name": "requirements-traceability",
      "description": "Link requirements to evidence, test cases, and planned coverage.",
      "category": "analysis"
    }
  ],
  "constraints": [
    {
      "type": "data_access",
      "rule": "No production database access",
      "scope": "strict"
    },
    {
      "type": "network",
      "rule": "Prefer staging/preview over production",
      "scope": "restricted"
    }
  ],
  "security_rules": [
    "No hardcoded credentials in test files",
    "Environment variables for base URLs and credentials"
  ],
  "performance_targets": {
    "agent_initialization_ms": 2000,
    "memory_usage_mb": 512
  }
}
