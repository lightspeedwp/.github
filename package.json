{
  "name": "lightspeed-github-org-control-plane",
  "version": "1.0.0",
  "description": "LightSpeed .github organization control plane",
  "scripts": {
    "test": "echo 'No tests configured'",
    "lint:md": "echo 'Markdown linting configured but not installed'",
    "lint:js": "echo 'JavaScript linting configured but not installed'",
    "lint:all": "echo 'All linting configured but not installed'",
    "format": "echo 'Code formatting configured but not installed'",
    "validate:frontmatter": "echo 'Frontmatter validation configured but not installed'",
    "validate:branch-name": "echo 'Branch name validation configured but not installed'"
  },
  "keywords": [
    "github",
    "organization",
    "control-plane"
  ],
  "author": "LightSpeed Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "eslint": "^8.50.0",
    "markdownlint": "^0.31.1",
    "prettier": "^3.0.0"
  }
}
