# WordPress Configuration Agent Export

Generated on 2026-07-14 in the current agent workspace.

This archive contains only resources that were readable from the current environment:

- Workspace agent instructions from `/workspace/AGENTS.md`
- Workspace agent assets from `/workspace/agent_files/`
- Runtime memory files directly under `/workspace/memory/`
- Locally readable skill folders under `/root/.codex/skills/`
- Locally readable plugin-provided skill folders under `/root/.codex/plugins/cache/`

The archive intentionally excludes hidden system/developer messages, protected platform internals, connector credentials, authentication tokens, and git internals. Connected app tools that expose no readable source files are listed as metadata-only records.

See `manifests/` for the file inventory, skill inventory, inaccessible-resource notes, redaction log, and validation notes.
