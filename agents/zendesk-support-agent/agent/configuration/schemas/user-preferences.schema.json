{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "User Preferences",
  "description": "Durable per-user or per-team preferences for repeated Zendesk support outputs.",
  "type": "object",
  "required": [
    "preferred_reply_tone",
    "preferred_report_sections",
    "preferred_timezone_or_date_formatting",
    "preferred_escalation_wording"
  ],
  "properties": {
    "preferred_reply_tone": {
      "type": "string",
      "minLength": 3,
      "description": "Standing tone preference for customer-facing replies."
    },
    "preferred_report_sections": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 3,
        "description": "A report section that should usually appear in repeated reports."
      },
      "minItems": 1,
      "uniqueItems": true,
      "description": "Sections that should usually appear in repeated reports."
    },
    "preferred_timezone_or_date_formatting": {
      "type": "string",
      "minLength": 3,
      "description": "Reusable timezone or date-formatting preference."
    },
    "preferred_escalation_wording": {
      "type": "string",
      "minLength": 3,
      "description": "Standing wording preference for escalation or urgency language."
    },
    "preferred_detail_level": {
      "type": "string",
      "minLength": 3,
      "description": "Preferred level of detail for repeated outputs."
    },
    "preferred_report_scope": {
      "type": "string",
      "minLength": 3,
      "description": "Standing scope preference for repeated reporting work."
    },
    "preferred_example_ticket_rule": {
      "type": "string",
      "minLength": 3,
      "description": "Standing rule for when example tickets should appear in reports."
    }
  },
  "additionalProperties": false
}
