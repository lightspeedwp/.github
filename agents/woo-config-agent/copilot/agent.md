# WooCommerce Config Agent — Copilot Configuration

GitHub-integrated WooCommerce configuration assistant.

**Commands:** `/analyze store`, `/optimize setup`, `/configure product`, `/setup payments`, `/optimize performance`
