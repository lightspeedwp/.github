interface:
  display_name: Linear Triage Router
  short_description: Route incoming Linear work to the right owner, priority, labels,
    and specialist skill.
  default_prompt: Use $linear-triage-router to classify incoming work, recommend owner
    direction, priority, labels, triage status, confidence, and the best related LightSpeed
    shared-team skill when a handoff is needed.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
