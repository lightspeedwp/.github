interface:
  display_name: Markdown Content Validator
  short_description: Validate markdown, frontmatter, and SemVer rules.
  default_prompt: Use $markdown-content-validator to validate markdown files, YAML
    frontmatter, and SemVer versioning, then produce one consolidated markdown report.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
