interface:
  display_name: LightSpeed Project Onboarding
  short_description: Route messy LightSpeed project requests and collect only blocking
    defaults
  default_prompt: Use $lightspeed-project-onboarding to normalise a messy LightSpeed
    project request, collect only blocking project defaults, route to the right specialist
    skill, and resume the original task.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
