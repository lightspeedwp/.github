# Sample Repo Analysis

## Repository
lightspeed-refill-store

## Findings
- Package manager: pnpm
- Playwright config present at `playwright.config.ts`
- Existing tests in `tests/e2e/`
- Custom theme lives in `wp-content/themes/lightspeed-refill/`
- Existing `data-pw` attributes appear in key CTA components
- GitHub Actions workflow runs `pnpm playwright test` against staging
