# Build Folder

This folder is used for compiled assets only (JS, CSS, etc.) and is generated by the build process (`npm run build` or `bin/build.sh`).

**Do NOT commit any built files to version control.**  
Only include this README or a `.gitkeep` to track the folder.

See [`PLACEHOLDER-WORKFLOW.single-block-plugin.md`](../PLACEHOLDER-WORKFLOW.single-block-plugin.md) for details.