// Entry point for {{slug}} block plugin JS.
// Use mustache placeholders for all dynamic values (e.g. {{slug}}, {{namespace}}).
// Example: import './{{slug}}/block.js';

// Register blocks and other JS functionality here.