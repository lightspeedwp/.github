# Placeholder Replacement Workflow

All template files containing mustache placeholders use the `.mustache` extension (e.g., `header.html.mustache`).  
After running the replacement script, rename or copy the files to remove `.mustache` for production use.

...