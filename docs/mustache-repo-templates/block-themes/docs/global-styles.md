# Global Styles

- Configure theme-wide settings in `theme.json`.
- Use `styles/` directory for style variations (e.g., dark mode).
- All style files should use mustache placeholders for colors, fonts, etc.

See [structure.md](structure.md) for details.