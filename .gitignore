# Dependencies
node_modules/
/vendor

# Build directories
/build
/dist
/out
metrics/out/

# WordPress specific
/wp-content/uploads
/wp-content/upgrade
/wp-content/backup-db
/wp-content/cache
/wp-content/backups

# Environment files
.env
.env.*
!.env.example

# IDE and editor files
# .vscode/*
# .vscode/extensions.json
# .vscode/settings.json
# .vscode/tasks.json
# .vscode/launch.json
# *.sublime-project
# *.sublime-workspace

# System files
.DS_Store
Icon?
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs
logs/
*.log
npm-debug.log*
.eslintcache
.phpcs-cache

# Agentic release-agent run logs (ephemeral output, not source)
.agentic-logs/

# Testing and coverage
/coverage
/.nyc_output

# Generator output
output-plugin/
generated-plugins/

# Skills registry (regenerated by scripts/validation/phase-6-skills-registry.js;
# embeds a run timestamp and machine-local absolute paths, so it is not reproducible
# across machines/CI runs and must not be committed)
skills/by-category/
skills/registry.json

# Same issue: phase-4/5/6 audit report output (timestamps + machine-local paths)
agents/reports/

# Misc
tmp/
*.zip
*.tar.gz
*.tgz
node_modules/
.claude/settings.local.json

# Prevent local Claude Code settings from being committed
.claude/settings*.json
.claude/.settings*.json
